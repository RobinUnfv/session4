server:
  port: ${CUSTOM.SERVER.PORT:7030}

spring:
  cloud:
    gateway:
      routes:
        - id: compras-service-route
          uri: lb://msvc-compras
          predicates:
            - Path=/api/compra/**
          filters:
            - StripPrefix=2