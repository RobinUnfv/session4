server:
  port: ${PORT_SERVICE_COMPRAS}

spring:
  cloud:
    config:
      uri: ${CONFIG_SERVER_CONF_URI}
      profile: ${CONFIG_SERVER_PROFILE}
      enabled: true
  
  mongodb:
    uri: ${BD_MONGODB_URI}
