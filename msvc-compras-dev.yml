server:
  port: 7020

spring:
  cloud:
    config:
      uri: http://config-server:9000
      profile: dev
      enabled: true

  data:
    mongodb:
      uri: ${BD_MONGODB_URI}

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka
